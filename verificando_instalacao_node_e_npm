//Para verificar a versão e se o Node está instalado, basta digitar no prompt de comando: node --version
//Para verificar a versão e se o NPM está instalado, basta digitar no prompt de comando: npm -version